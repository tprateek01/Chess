# Copy this to .env.local for local development (vercel dev reads it).
# .env.local is gitignored — never put your real connection string in a
# file that gets committed. In production, set DATABASE_URL in
# Vercel → Project → Settings → Environment Variables instead.

DATABASE_URL=postgres://<user>:<password>@<host>/<database>?sslmode=require
